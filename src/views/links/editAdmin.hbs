<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card">
                <div class="card-body">
                    <form id="form-edit-user" action="/usuarios/edit/{{user.id}}" method="POST">
                        <div class="form-group">
                            <input id="username-edit-user" type="text" value="{{user.username}}" class="form-control" name="username"
                                placeholder="username" autofocus>
                        </div>
                        <div class="form-group">
                            <input id="password-edit-user" type="password" name="password" class="form-control"
                                placeholder="Contraseña(Default)">
                        </div>
                        <div class="form-group">
                            <input id="fullname-edit-user" type="text" value="{{user.fullname}}" name="fullname" class="form-control"
                                placeholder="Nombre Completo">
                        </div>
                        <div class="form-group">
                            <button class="btn btn-success btn-block">
                                Guardar
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>